/**
 * ARGUS ESP32 Firmware
 * Generated device-specific entry point.
 * Infrastructure is provided by the ARGUS SDK library.
 */

#include <argus.h>

// ================= CONFIGURATION =================

#if {{.MQTTPort}} == 8883
#define ARGUS_MQTT_SECURE 1
#endif

char ARGUS_DEVICE_ID[] = "{{cppString .DeviceID}}";
char ARGUS_API_KEY[] = "{{cppString .APIKey}}";
char ARGUS_SERVER_HOST[] = "{{cppString .ServerHost}}";
uint16_t ARGUS_HTTP_PORT = {{.HTTPPort}};
char ARGUS_MQTT_HOST[] = "{{cppString .ServerHost}}";
uint16_t ARGUS_MQTT_PORT = {{.MQTTPort}};
char WIFI_SSID[] = "{{cppString .WiFiSSID}}";
char WIFI_PASSWORD[] = "{{cppString .WiFiPassword}}";
char ARGUS_FW_VERSION[] = "{{cppString .FirmwareVersion}}";
char ARGUS_OTA_KEY_ID[] = "{{cppString .OTASigningKeyID}}";
char ARGUS_OTA_PUBLIC_KEY_B64[] = "{{cppString .OTASigningPublicKeyB64}}";

char ARGUS_ROOT_CA[] PROGMEM = R"EOF(
{{.RootCAPEM}})EOF";

char ARGUS_DEVICE_CERT[] PROGMEM = R"EOF(
{{.DeviceCertPEM}})EOF";

char ARGUS_DEVICE_PRIVATE_KEY[] PROGMEM = R"EOF(
{{.DevicePrivateKeyPEM}})EOF";

// ================= USER CODE =================

{{if .UserCode}}
{{.UserCode}}
{{else}}
void setup()
{
    Serial.begin(115200);
    argusBegin();
}

void loop()
{
    argusLoop();
}
{{end}}
